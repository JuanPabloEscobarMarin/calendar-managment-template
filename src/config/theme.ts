/**
 * Este archivo define un objeto de tema básico.  Puedes modificar los valores
 * aquí (o cargar distintos temas) para cambiar los colores y estilos
 * generales de la aplicación sin tocar los componentes.  Tailwind utiliza
 * clases utilitarias, por lo que aquí se definen los nombres de colores
 * (por ejemplo `indigo-600`) que luego se asignan en los componentes.
 */

export const theme = {
  colors: {
    /** Color principal usado en botones y enlaces activos */
    primary: "indigo-600",
    /** Color secundario para elementos destacados */
    secondary: "pink-500",
    /** Color de acento para botones secundarios o detalles */
    accent: "emerald-500",
  },
};
